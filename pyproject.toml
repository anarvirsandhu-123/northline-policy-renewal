[project]
name = "northline-policy-renewal"
version = "0.1.0"
description = "DEV-only Boomi first slice: PolicyRenewalNotification → POST /v2/renewals"
requires-python = ">=3.11"
readme = "README.md"
